# KevStack AI Backend

## Run

1. Copy `env.example` to `.env`
2. Install dependencies:
   ```bash
   npm install
   ```
3. Start server:
   ```bash
   npm run start
   ```

Server runs on `http://localhost:4000` by default.
